{
  "name": "lx-web-music",
  "version": "1.0.0",
  "private": true,
  "description": "Online music player for Cloudflare Pages using lx-music-source backends",
  "scripts": {
    "dev": "wrangler pages dev . --compatibility-date=2024-11-01",
    "deploy": "wrangler pages deploy . --project-name=lx-web-music"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
